\documentclass[tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{calc, positioning}

\begin{document}
\begin{tikzpicture}[
    font=\sffamily,
    node distance=1.8cm and 3.8cm,
    every node/.style={
        draw,
        thick,
        rounded corners=4pt,
        minimum width=3cm,
        minimum height=1.2cm,
        align=center
    },
    base/.style={draw=black, fill=white},
    found/.style={base, fill=blue!10},
    dynamic/.style={base, fill=green!15},
    applied/.style={base, fill=orange!15},
    appendix/.style={base, fill=gray!10, font=\small},
    every path/.style={thick, draw=black!70, ->, >=latex},
    dashed path/.style={thick, draw=black!40, dashed, ->, >=latex}
]

% Legend in top-left corner
\node[anchor=west, draw=none] at (-10, 5) {\textbf{Legend}};
\draw[found] (-10, 4.3) rectangle ++(0.5, 0.5);
\node[draw=none] at (-8.3, 4.55) {Core concepts};

\draw[dynamic] (-10, 3.5) rectangle ++(0.5, 0.5);
\node[draw=none] at (-7.6, 3.75) {Dynamic / evolutionary};

\draw[applied] (-10, 2.7) rectangle ++(0.5, 0.5);
\node[draw=none] at (-7.9, 2.95) {Applied / extended};

\draw[appendix] (-10, 1.9) rectangle ++(0.5, 0.5);
\node[draw=none] at (-8.6, 2.15) {Appendix};

% ── Core game theory foundations ─────────────────────────────────────────────
\node[found] (games)          at (0, 0)           {Chapter 1\\Games};
\node[found] (rationalisation)[right=of games]     {Chapter 2\\Rationalisation};
\node[found] (zero_sum)       [below=of rationalisation] {Chapter 3\\Zero-Sum Games};
\node[found] (nash)           [right=of zero_sum]  {Chapter 4\\Nash Equilibrium};
\node[found] (brp)            [above=of nash]      {Chapter 12\\Best Response\\Polytopes};
\node[found] (subgame)        [right=of brp]       {Chapter 5\\Subgame Perfection};

% ── Dynamic and evolutionary chapters ────────────────────────────────────────
\node[dynamic] (repeated)     [right=of nash]      {Chapter 6\\Repeated Games};
\node[dynamic] (direct_recip) [right=of repeated]  {Chapter 7\\Direct Reciprocity};
\node[dynamic] (replicator)   [below=of nash]      {Chapter 9\\Replicator Dynamics};
\node[dynamic] (moran)        [right=of replicator]{Chapter 10\\Moran Process};
\node[dynamic] (evol_bio)     [right=of moran]     {Chapter 8\\Evolutionary\\Biology};
\node[dynamic] (further)      [below=of evol_bio]  {Chapter 11\\Further Learning\\Dynamics};

% ── Applied chapters ──────────────────────────────────────────────────────────
\node[applied] (routing)      [left=of replicator] {Chapter 13\\Routing Games};
\node[applied] (auction)      [left=of routing]    {Chapter 15\\Auction Games};
\node[applied] (matching)     [below=of auction]   {Chapter 14\\Matching Games};
\node[applied] (social)       [below=of matching]  {Chapter 16\\Social Choice};
\node[applied] (coop)         [right=of social]    {Chapter 17\\Cooperative Games};

% ── Appendices ────────────────────────────────────────────────────────────────
\node[appendix] (numint)   [below=of replicator] {Appendix\\Numerical Integration};
\node[appendix] (absorb)   [below=of moran]      {Appendix\\Absorbing\\Markov Chains};
\node[appendix] (ergodic)  [below=of further]    {Appendix\\Ergodic\\Markov Chains};
\node[appendix] (ip)       [below=of routing]    {Appendix\\Interior Point Opt.};
\node[appendix] (intpivot) [above=of brp]        {Appendix\\Integer Pivoting};

% ── Edges: foundations ───────────────────────────────────────────────────────
\draw (games)          -- (rationalisation);
\draw (rationalisation)-- (zero_sum);
\draw (rationalisation)-- (nash);
\draw (zero_sum)       -- (nash);
\draw (nash)           -- (subgame);
\draw (nash)           -- (brp);
\draw (nash)           -- (repeated);
\draw (nash)           -- (replicator);
\draw (nash)           -- (routing);
\draw (nash)           -- (auction);
\draw (subgame)        -- (repeated);

% ── Edges: dynamics ──────────────────────────────────────────────────────────
\draw (repeated)       -- (direct_recip);
\draw (direct_recip)   -- (evol_bio);
\draw (evol_bio)       -- (moran);
\draw[<->] (replicator)-- (moran);
\draw (moran)          -- (further);
\draw (replicator)     -- (further);
\draw (evol_bio)       -- (further);

% ── Edges: applied ───────────────────────────────────────────────────────────
\draw[<->] (matching)  -- (social);

% ── Appendix edges (dashed) ──────────────────────────────────────────────────
\draw[dashed path] (numint)   -- (replicator);
\draw[dashed path] (absorb)   -- (moran);
\draw[dashed path] (ergodic)  -- (moran);
\draw[dashed path] (ergodic)  -- (further);
\draw[dashed path] (intpivot) -- (brp);
\draw[dashed path] (intpivot) -- (zero_sum);
\draw[dashed path] (ip)       -- (routing);

\end{tikzpicture}
\end{document}
